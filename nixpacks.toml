[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[variables]
# Prevent Playwright from downloading browsers (saves ~200MB and minutes)
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "1"
# Only install production deps at root level
NODE_ENV = "production"
